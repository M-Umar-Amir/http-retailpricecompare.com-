/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'http://retailpricecompare.com/', // Update this!
    generateRobotsTxt: true, // Create robots.txt
    generateIndexSitemap: false,
  };
  
  module.exports = config;
  